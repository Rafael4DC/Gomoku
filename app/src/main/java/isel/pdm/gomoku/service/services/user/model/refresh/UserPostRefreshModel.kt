package pt.isel.daw.gomokuroyale.http.controller.user.model.refresh

/**
 * Represents a user post refresh model.
 *
 * @property refreshToken The user's refresh token.
 */
data class UserPostRefreshModel(
    val refreshToken: String
)
